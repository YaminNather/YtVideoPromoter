import {StyleSheet} from "react-native";

export const CommonStyles = StyleSheet.create(
  {
    Title: {fontSize: 40, fontWeight: "bold"},
    Button: {borderRadius: 20},
    Container: {width: "100%", padding: 20, borderRadius: 20, alignItems: "center"}
  }
);